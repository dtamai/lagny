@namespace("Kerala")
protocol Kerala {
    import idl "Header_v1.avdl";

    record AddOrUpdateSeller {
        Header header = {};
        string identifier = "unknown";
        string display_name = "unknown";
    }
}